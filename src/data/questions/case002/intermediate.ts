import { authoredSet } from '../../curriculum/authoring';

export const QUESTION_SET = authoredSet('002', 'intermediate', [
  {
    title: 'Bucket the incident hours', prompt: 'Keep Heartbeat at or after March 11 09:00Z. Count Beats by one-hour TimeGenerated bins and sort the bins ascending.',
    body: 'bin rounds timestamps down to bucket boundaries. Here the inclusive 09:00 start adds the boundary observations; do not substitute the earlier strict ago(3h) filter.',
    pattern: 'Heartbeat | where <time filter> | summarize Beats = count() by bin(TimeGenerated, 1h) | sort by TimeGenerated asc',
    example: { query: 'Heartbeat | where TimeGenerated >= datetime(2026-03-11 11:00:00) | summarize Beats = count() by bin(TimeGenerated, 1h)', explain: 'The 11:00 hour has 84 observations from seven reporting machines.' },
    solution: 'Heartbeat | where TimeGenerated >= datetime(2026-03-11 09:00:00) | summarize Beats = count() by bin(TimeGenerated, 1h) | sort by TimeGenerated asc',
    operators: ['where', 'summarize', 'count', 'bin', 'sort'], ordered: true,
    hints: ['Include the 09:00 rows with >=.', 'Group by bin(TimeGenerated, 1h), then sort ascending.'],
    sourceIds: ['MLKQL-Part12', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-005-01',
    evidence: { title: 'An hourly step down', detail: 'The 09:00 bucket has 104 beats; 10:00 and 11:00 each have 84. Hour bins expose the change but blur its exact minute.', chainIndex: 2 },
  },
  {
    title: 'Chart the symptom burst', prompt: 'Filter Syslog to MTU_MISMATCH, count Messages in one-minute TimeGenerated bins, sort ascending and request a timechart.',
    body: 'Aggregate before render so the visualization describes the intended grain. render timechart attaches presentation metadata; it does not create missing points or prove a cause.',
    pattern: 'Syslog | where <term filter> | summarize Messages = count() by bin(TimeGenerated, 1m) | sort by TimeGenerated asc | render timechart',
    example: { query: 'Syslog | where SyslogMessage has "MTU_MISMATCH" | summarize Messages = count() by bin(TimeGenerated, 1m)', explain: 'The unrendered table has two messages in each of three minute bins.' },
    solution: 'Syslog | where SyslogMessage has "MTU_MISMATCH" | summarize Messages = count() by bin(TimeGenerated, 1m) | sort by TimeGenerated asc | render timechart',
    operators: ['where', 'has', 'summarize', 'count', 'bin', 'sort', 'render'], ordered: true,
    hints: ['Use the exact term, not the broader mismatch substring.', 'Bin by 1m and render the sorted grouped result.'],
    sourceIds: ['MLKQL-Part12'], sourceTerminalId: 'T-005-02',
    adaptation: 'Timeline chart uses the existing Syslog burst; rendering remains metadata over executed results.',
    evidence: { title: 'Three adjacent symptom minutes', detail: 'Two messages fall in each of the 09:13, 09:14 and 09:15 minute bins, shortly after the 09:12Z write.', chainIndex: 1 },
  },
  {
    title: 'Split the hourly signal', prompt: 'For Heartbeat at or after 09:00Z on March 11, count Beats by one-hour TimeGenerated bins and ComputerEnvironment. Sort by time then environment ascending; request a columnchart.',
    body: 'Adding an environment grouping prevents one healthy population from hiding another population’s decline. Columnchart metadata changes presentation, not grouped values.',
    pattern: '<Table> | summarize Beats = count() by bin(TimeGenerated, 1h), ComputerEnvironment | render columnchart',
    example: { query: 'Heartbeat | where TimeGenerated >= datetime(2026-03-11 10:00:00) | summarize Beats = count() by ComputerEnvironment', explain: 'After 10:00Z, Production has 96 beats, Development 48, and UAT 24.' },
    solution: 'Heartbeat | where TimeGenerated >= datetime(2026-03-11 09:00:00) | summarize Beats = count() by bin(TimeGenerated, 1h), ComputerEnvironment | sort by TimeGenerated asc, ComputerEnvironment asc | render columnchart',
    operators: ['where', 'summarize', 'count', 'bin', 'sort', 'render'], ordered: true,
    hints: ['Keep two grouping keys: the hour and the environment.', 'Sort the hour and environment independently before rendering.'],
    sourceIds: ['MLKQL-Part12', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-005-03',
    adaptation: 'Two-day comparison adapted to three incident hours and environment series; no fabricated second-day comparison.',
    evidence: { title: 'Production falls, controls stay level', detail: 'Production declines from 68 beats to 48 per hour. Development stays at 24 and UAT at 12 per hour.', chainIndex: 2 },
  },
  {
    title: 'The bucket that never exists', prompt: 'Compare PRD-SQL-01 and healthy PRD-SQL-02 between 09:15Z and 09:30Z inclusive on March 11. Return Beats = count() grouped by Computer.',
    body: 'summarize emits only groups with data; where Beats == 0 cannot reveal a missing bucket. Compare a known healthy host over the same explicit window instead. This is not make-series or gap filling.',
    pattern: 'Heartbeat | where Computer in ("<affected>", "<control>") | where TimeGenerated between (...) | summarize Beats = count() by Computer',
    example: { query: 'Heartbeat | where Computer == "PRD-SQL-01" and TimeGenerated > datetime(2026-03-11 09:15:00) | summarize Beats = count() by bin(TimeGenerated, 15m)', explain: 'There are no groups after the cutoff, not zero-valued buckets.', allowEmpty: true },
    solution: 'Heartbeat | where Computer in ("PRD-SQL-01", "PRD-SQL-02") | where TimeGenerated between (datetime(2026-03-11 09:15:00) .. datetime(2026-03-11 09:30:00)) | summarize Beats = count() by Computer',
    operators: ['where', 'between', 'summarize', 'count'],
    hints: ['Include the last 09:15 observation so both hosts have a group.', 'Compare the two named hosts over one inclusive 15-minute window.'],
    sourceIds: ['MLKQL-Part12', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-005-04',
    adaptation: 'Preserves missing-bucket lesson using a healthy-host comparison; no make-series, interpolation or precomputed output.',
    evidence: { title: 'One observation versus four', detail: 'SQL-01 has one beat at the boundary; SQL-02 has four. No additional affected-host buckets are generated after 09:15Z.', chainIndex: 2 },
  },
  {
    title: 'Measure the lead time', prompt: 'For NetworkChanges CHG-4471, calculate MinutesToLastBeat with datetime_diff from the fixed 09:15Z last heartbeat back to TimeGenerated. Report TimeGenerated, ChangeId, MinutesToLastBeat and Message.',
    body: 'datetime_diff takes the later value before the earlier value for a positive elapsed interval. A three-minute lead supports the sequence; the rule payload supplies the mechanism.',
    pattern: '<Table> | extend MinutesToLastBeat = datetime_diff("minute", datetime(<last beat>), TimeGenerated) | project <columns>',
    example: { query: 'NetworkChanges | where ChangeId == "CHG-4471" | extend SecondsToLastBeat = datetime_diff("second", datetime(2026-03-11 09:15:00), TimeGenerated) | project SecondsToLastBeat', explain: 'The interval is 180 seconds; changing the unit changes the scale.' },
    solution: 'NetworkChanges | where ChangeId == "CHG-4471" | extend MinutesToLastBeat = datetime_diff("minute", datetime(2026-03-11 09:15:00), TimeGenerated) | project TimeGenerated, ChangeId, MinutesToLastBeat, Message',
    operators: ['where', 'extend', 'datetime_diff', 'project'],
    hints: ['Filter the incident change, not the baseline.', 'Subtract the change time from the known last-beat boundary using the minute unit.'],
    sourceIds: ['MLKQL-Part12', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-005-05',
    adaptation: 'Exact-boundary outline adapted to a measured change-to-heartbeat interval; no join or hidden clock.',
    evidence: { title: 'Three minutes before last contact', detail: 'The 09:12Z deny change leads the 09:15Z last heartbeat by three minutes. The record explicitly says outbound Deny on port 443.', chainIndex: 1 },
  },
]);
