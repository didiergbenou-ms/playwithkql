import { authoredSet } from '../../curriculum/authoring';

export const QUESTION_SET = authoredSet('002', 'expert', [
  {
    title: 'Audit the administrator context', prompt: 'In SigninLogs, keep Dana’s successful sign-ins before March 11 09:12Z. Report TimeGenerated, UserPrincipalName, OS = tostring(DeviceDetail.operatingSystem), Managed = DeviceDetail.isManaged and Method = tostring(AuthenticationDetails[0].method).',
    body: 'Filter identity, result and time before reading dynamic context. These are authored lab sign-ins, not evidence of a real compromise. A success code and managed device are context, not proof of intent.',
    pattern: 'SigninLogs | where <identity, result and time> | project <entity and dynamic context>',
    example: { query: 'SigninLogs | where ResultType != 0 | project UserPrincipalName, ResultType', explain: 'One synthetic Dana record requests additional authentication; it is not labelled an attack.' },
    solution: 'SigninLogs | where UserPrincipalName == "dana.whitfield@contoso.com" and ResultType == 0 and TimeGenerated < datetime(2026-03-11 09:12:00) | project TimeGenerated, UserPrincipalName, OS = tostring(DeviceDetail.operatingSystem), Managed = DeviceDetail.isManaged, Method = tostring(AuthenticationDetails[0].method)',
    operators: ['where', 'project', 'tostring'],
    hints: ['The successful result code is numeric zero.', 'Read the device bag and the first authentication step only after the filters.'],
    sourceIds: ['MLKQL-Part20', 'MSLearn-path-sc200'], sourceTerminalId: 'T-011-01',
    adaptation: 'SecurityEvent-shape outline adapted to synthetic admin sign-in context; no missing SecurityEvent table or hacked account is invented.',
    evidence: { title: 'Two successful pre-change sessions', detail: 'Dana’s 08:42Z and 09:05Z rows show Windows, Managed=true and MFA. This gives audit context, not a malicious-intent finding.', chainIndex: 0 },
  },
  {
    title: 'Separate friction from compromise', prompt: 'For each SigninLogs UserPrincipalName, report Attempts = count(), Failures = countif(ResultType != 0), and Addresses = dcount(IPAddress).',
    body: 'Aggregate identity evidence before escalating. A nonzero result can be authentication friction; a single observed address is not a geolocation or trusted-network proof.',
    pattern: 'SigninLogs | summarize Attempts = count(), Failures = countif(ResultType != 0), Addresses = dcount(IPAddress) by UserPrincipalName',
    example: { query: 'SigninLogs | summarize Locations = dcount(Location), Addresses = dcount(IPAddress)', explain: 'The authored fixture has one location label and two documentation addresses; no IP lookup is executed.' },
    solution: 'SigninLogs | summarize Attempts = count(), Failures = countif(ResultType != 0), Addresses = dcount(IPAddress) by UserPrincipalName',
    operators: ['summarize', 'count', 'countif', 'dcount'],
    hints: ['Keep failed rows in the input and count them conditionally.', 'Group by user, not IP, and use dcount for address diversity.'],
    sourceIds: ['MLKQL-Part20', 'MSLearn-path-sc200'], sourceTerminalId: 'T-011-02',
    adaptation: 'Replaces unsupported CIDR/geolocation hunting with observed identity aggregates; does not claim IP-range or location analysis.',
    evidence: { title: 'No travelling-account claim', detail: 'Dana has four attempts, one nonzero result and one address; automation has two attempts, zero nonzero results and one address. These fixtures do not establish compromise.', chainIndex: 0 },
  },
  {
    title: 'Latest successful identity evidence', prompt: 'Keep successful SigninLogs at or before March 11 09:12Z. For each UserPrincipalName use arg_max to carry the latest TimeGenerated, IPAddress and IsApprovedAdmin.',
    body: 'Select the most recent successful row within the investigation window. Matching Dana’s identity to the audit is a human evidence comparison here, not an executed join or threat-intelligence correlation.',
    pattern: 'SigninLogs | where <success and fixed time> | summarize arg_max(TimeGenerated, IPAddress, IsApprovedAdmin) by UserPrincipalName',
    example: { query: 'SigninLogs | where UserPrincipalName == "dana.whitfield@contoso.com" and ResultType == 0 | summarize SuccessfulSessions = count()', explain: 'Three successful Dana rows exist overall, but the 10:00Z one is after the change.' },
    solution: 'SigninLogs | where ResultType == 0 and TimeGenerated <= datetime(2026-03-11 09:12:00) | summarize arg_max(TimeGenerated, IPAddress, IsApprovedAdmin) by UserPrincipalName',
    operators: ['where', 'summarize', 'arg_max'],
    hints: ['Exclude the post-change sessions before selecting latest.', 'Carry both IPAddress and IsApprovedAdmin from the winning row.'],
    sourceIds: ['MLKQL-Part20', 'MSLearn-path-sc200'], sourceTerminalId: 'T-011-03',
    adaptation: 'Replaces threat-intelligence join with explicit latest successful identity context; no join, lookup or TI coverage claimed.',
    evidence: { title: 'Approved identity before the write', detail: 'Dana’s latest successful pre-change sign-in is 09:05Z; automation’s is 08:00Z. Both are marked approved administrators in the synthetic fixture.', chainIndex: 0 },
  },
  {
    title: 'One entity, one review candidate', prompt: 'In AzureActivity between 09:00Z and 09:20Z inclusive, keep successful SECURITYRULES/WRITE operations. Count Writes by Caller and _ResourceId, then keep groups with Writes >= 1.',
    body: 'A grouped candidate avoids returning one alert per raw event. A threshold of one is intentional for network-rule review in this small lab, not a recommended production security rule.',
    pattern: 'AzureActivity | where <fixed window and operation> | summarize Writes = count() by Caller, _ResourceId | where Writes >= 1',
    example: { query: 'AzureActivity | where OperationNameValue contains "SECURITYRULES/WRITE" | summarize Writes = count() by Caller', explain: 'The supplied audit has one write by Dana; it says nothing about unauthorized access.' },
    solution: 'AzureActivity | where TimeGenerated between (datetime(2026-03-11 09:00:00) .. datetime(2026-03-11 09:20:00)) | where ActivityStatusValue == "Succeeded" and OperationNameValue contains "SECURITYRULES/WRITE" | summarize Writes = count() by Caller, _ResourceId | where Writes >= 1',
    operators: ['where', 'between', 'contains', 'summarize', 'count'],
    hints: ['Scope time and operation before grouping.', 'Group by both caller and resource, then apply the review threshold to Writes.'],
    sourceIds: ['MLKQL-Part20', 'MSLearn-path-sc200'], sourceTerminalId: 'T-011-04',
    evidence: { title: 'A single account-resource candidate', detail: 'The result is one Dana / nsg-prod-outbound group with Writes=1. It identifies a configuration-review candidate, not an attack.', chainIndex: 0 },
  },
  {
    title: 'Project a review-ready rule change', prompt: 'In NetworkChanges between 09:00Z and 09:20Z inclusive, keep approved changes whose parsed after rule is Deny on port 443. Report TimeGenerated, Account = Caller, AzureResource = _ResourceId, ChangeId and Port = toint(p.after.port).',
    body: 'A review query needs explicit entity columns and a reproducible window. This fixed training window is not a scheduled analytics rule: production periods, frequency, deduplication and false-positive policy require separate design.',
    pattern: 'NetworkChanges | where <fixed window> | extend p = parse_json(Properties) | where <approved deny rule> | project <time and entity columns>',
    example: { query: 'NetworkChanges | where Approved == true | summarize ApprovedChanges = count()', explain: 'All three fixtures are approved, demonstrating that approval alone does not distinguish the unsafe change.' },
    solution: 'NetworkChanges | where TimeGenerated between (datetime(2026-03-11 09:00:00) .. datetime(2026-03-11 09:20:00)) | extend p = parse_json(Properties) | where Approved == true and tostring(p.after.access) == "Deny" and toint(p.after.port) == 443 | project TimeGenerated, Account = Caller, AzureResource = _ResourceId, ChangeId, Port = toint(p.after.port)',
    operators: ['where', 'between', 'extend', 'parse_json', 'tostring', 'toint', 'project'],
    hints: ['Approval is a boolean field, not the string "true".', 'Filter the parsed rule and project the two renamed entity columns.'],
    sourceIds: ['MLKQL-Part20', 'MSLearn-path-sc200'], sourceTerminalId: 'T-011-05',
    adaptation: 'Entity-mapping outline becomes a local review query only; no deployed Sentinel rule, watchlist, geolocation or Microsoft library.',
    evidence: { title: 'Authorized identity, unsafe path', detail: 'The review result names Dana, the production NSG and CHG-4471 with numeric port 443 at 09:12Z. The problem is the changed path, not a proven stolen account.', chainIndex: 1 },
  },
]);
