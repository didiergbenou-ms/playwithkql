import { authoredSet } from '../../curriculum/authoring';

export const QUESTION_SET = authoredSet('003', 'intermediate', [
  {
    title: 'Convert the memory unit', prompt: 'Keep Available MBytes in Perf, calculate AvailableGB = CounterValue / 1024, and report TimeGenerated, Computer and AvailableGB.',
    body: 'extend adds a calculated column; project chooses the report shape. Divide this counter by 1024 only after filtering to memory, never across mixed units.',
    pattern: 'Perf | where <memory filter> | extend AvailableGB = CounterValue / 1024 | project <columns>',
    example: { query: 'Perf | where CounterName == "Available MBytes" | take 2 | extend AvailableGB = CounterValue / 1024', explain: 'The original memory number and converted number remain side by side until projected.' },
    solution: 'Perf | where CounterName == "Available MBytes" | extend AvailableGB = CounterValue / 1024 | project TimeGenerated, Computer, AvailableGB',
    operators: ['where', 'extend', 'project'],
    hints: ['Select Available MBytes exactly.', 'Divide by 1024 in extend, then keep the three requested columns.'],
    sourceIds: ['MLKQL-Part13', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-006-01',
    evidence: { title: 'Memory measurements remain independent', detail: 'The report contains 1,152 converted memory observations. Unit conversion makes them readable; it does not diagnose the heartbeat path.', chainIndex: 3 },
  },
  {
    title: 'Signed time offsets', prompt: 'Take the top five CPU samples by CounterValue descending. Add MinutesFromChange relative to March 11 09:12Z, and report Computer, TimeGenerated, CounterValue and MinutesFromChange.',
    body: 'A negative datetime_diff means an observation precedes the reference event. Preserve the sign: a peak before a change cannot be caused by that later change.',
    pattern: 'Perf | where <CPU filter> | top 5 by CounterValue desc | extend MinutesFromChange = datetime_diff("minute", TimeGenerated, datetime(<change>))',
    example: { query: 'NetworkChanges | where ChangeId == "CHG-4471" | extend MinutesToClock = datetime_diff("minute", now(), TimeGenerated) | project MinutesToClock', explain: 'At the fixed noon clock, the change was 168 minutes ago.' },
    solution: 'Perf | where CounterName == "% Processor Time" | top 5 by CounterValue desc | extend MinutesFromChange = datetime_diff("minute", TimeGenerated, datetime(2026-03-11 09:12:00)) | project Computer, TimeGenerated, CounterValue, MinutesFromChange',
    operators: ['where', 'top', 'extend', 'datetime_diff', 'project'], ordered: true,
    hints: ['Filter to CPU, then rank the five peaks.', 'Place TimeGenerated before the change timestamp in datetime_diff to retain negative offsets.'],
    sourceIds: ['MLKQL-Part13', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-006-02',
    adaptation: 'Duration outline uses observed peak offsets; no invented AzureActivity StartTime or EndTime fields.',
    evidence: { title: 'Two peaks precede the write', detail: 'In rank order the offsets are +3, +3, -12, +3 and -12 minutes. The earlier web-server peaks are not consequences of the 09:12Z change.', chainIndex: 3 },
  },
  {
    title: 'Label without discarding', prompt: 'For every CPU sample, label Band as Review when CounterValue >= 95, otherwise Baseline, using case(). Return Samples = count() by Band.',
    body: 'case tests conditions in order and needs a fallback value. Lab thresholds are descriptive, not validated anomaly models; keep both categories to retain the denominator.',
    pattern: '<Table> | extend Band = case(<condition>, "Review", "Baseline") | summarize Samples = count() by Band',
    example: { query: 'Perf | where CounterName == "% Processor Time" | top 3 by CounterValue desc | extend Band = case(CounterValue >= 99, "Highest", "Other") | project Computer, Band', explain: 'The first threshold marks SQL-01 without claiming a root cause.' },
    solution: 'Perf | where CounterName == "% Processor Time" | extend Band = case(CounterValue >= 95, "Review", "Baseline") | summarize Samples = count() by Band',
    operators: ['where', 'extend', 'case', 'summarize', 'count'],
    hints: ['Do not filter to only the peaks; keep every CPU sample.', 'Use Review and Baseline as the two labels, then count by Band.'],
    sourceIds: ['MLKQL-Part13'], sourceTerminalId: 'T-006-03',
    evidence: { title: 'Five of 1,152 samples cross the threshold', detail: 'Review contains five CPU samples; Baseline contains 1,147. A threshold labels observations, not their cause.', chainIndex: 3 },
  },
  {
    title: 'Build the audit headline', prompt: 'For NetworkChanges CHG-4471, return Headline = Caller + " applied " + Ticket + ": " + Message, using strcat.',
    body: 'A readable headline should preserve the responsible identity, ticket and observed action. Concatenation must not turn an authorized configuration error into a claim of account compromise.',
    pattern: '<Table> | extend Headline = strcat(<parts>) | project Headline',
    example: { query: 'NetworkChanges | where ChangeId == "BASELINE-0800" | extend Headline = strcat(ChangeId, ": ", Message) | project Headline', explain: 'The baseline headline reports Allow, not the later Deny.' },
    solution: 'NetworkChanges | where ChangeId == "CHG-4471" | extend Headline = strcat(Caller, " applied ", Ticket, ": ", Message) | project Headline',
    operators: ['where', 'extend', 'strcat', 'project'],
    hints: ['Read all headline parts from the incident record.', 'Keep spaces and punctuation exactly as requested, including the colon after the ticket.'],
    sourceIds: ['MLKQL-Part13'], sourceTerminalId: 'T-006-04',
    adaptation: 'Uses recorded string fields instead of fabricating operation duration columns.',
    evidence: { title: 'An attributable configuration action', detail: 'The headline names dana.whitfield@contoso.com, CHG-4471 and outbound Deny on port 443. Intent is not inferred.', chainIndex: 1 },
  },
  {
    title: 'Measure continuing observations', prompt: 'For the five silent hosts’ CPU samples strictly after March 11 09:15Z, calculate MinutesAfterLastBeat. Return Machines = dcount(Computer), Samples = count() and LatestGapMinutes = max(MinutesAfterLastBeat).',
    body: 'An explicit time boundary and host list make an independent-export comparison reproducible. This adaptation repeats constants rather than teaching unsupported let; no hidden cross-table join is performed.',
    pattern: 'Perf | where <counter, host and time filters> | extend MinutesAfterLastBeat = datetime_diff("minute", TimeGenerated, datetime(<cutoff>)) | summarize <aggregations>',
    example: { query: 'Perf | where CounterName == "% Processor Time" and Computer == "PRD-SQL-01" | summarize LastPerf = max(TimeGenerated)', explain: 'The independent CPU export reaches 11:45Z for SQL-01.' },
    solution: 'Perf | where CounterName == "% Processor Time" | where Computer in ("PRD-WEB-01", "PRD-WEB-02", "PRD-APP-01", "PRD-APP-02", "PRD-SQL-01") | where TimeGenerated > datetime(2026-03-11 09:15:00) | extend MinutesAfterLastBeat = datetime_diff("minute", TimeGenerated, datetime(2026-03-11 09:15:00)) | summarize Machines = dcount(Computer), Samples = count(), LatestGapMinutes = max(MinutesAfterLastBeat)',
    operators: ['where', 'extend', 'datetime_diff', 'summarize', 'dcount', 'count', 'max'],
    hints: ['Filter the CPU counter and all five affected identities before calculating.', 'Exclude 09:15 itself. Aggregate the gap after extending it.'],
    sourceIds: ['MLKQL-Part13', 'MSLearn-path-monitoring'], sourceTerminalId: 'T-006-05',
    adaptation: 'Replaces let/readability outline with explicit constants and an independent-export calculation; no let coverage claimed.',
    evidence: { title: 'Samples continue beyond heartbeat loss', detail: 'All five hosts have 50 later CPU samples in total, reaching 150 minutes beyond the last heartbeat. The Perf export is not evidence of successful workspace heartbeat delivery.', chainIndex: 3 },
  },
]);
