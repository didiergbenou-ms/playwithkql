import { HEARTBEAT_HILLS } from '../../game/levels/heartbeatHills';
import { createCurriculumCase } from '../curriculum/narrative';
import { QUESTION_SET } from '../questions/case001/beginner';

export const CASE001 = createCurriculumCase('001', 'Heartbeat Hills', HEARTBEAT_HILLS, QUESTION_SET);
